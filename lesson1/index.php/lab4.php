:دوال التاريخ و الوقت مع شرح مبسط*

---

*🕒 1. date()
تُستخدم لعرض التاريخ أو الوقت بصيغة معينة.

```php
<?php
echo date("Y-m-d H:i:s"); // 2025-12-19 23:35:00
?>
```

*أشهر التنسيقات:*
- `Y` → السنة (2025)
- `m` → الشهر (01 إلى 12)
- `d` → اليوم (01 إلى 31)
- `H` → الساعة بصيغة 24 (00 إلى 23)
- `i` → الدقائق
- `s` → الثواني

---

*🗓️ 2. time()
ترجع عدد الثواني منذ 1 يناير 1970 (Unix Timestamp).

```php
<?php
echo time(); // مثال: 1766181300
?>
```

---

*⏳ 3. mktime()
تُستخدم لإنشاء طابع زمني (timestamp) من تاريخ ووقت معين.

```php
<?php
$timestamp = mktime(15, 30, 0, 12, 25, 2025);
echo date("Y-m-d H:i:s", $timestamp); // 2025-12-25 15:30:00
?>
```

---

*📆 4. strtotime()
تحوّل سلسلة نصية إلى طابع زمني.

```php
<?php
$ts = strtotime("next Friday");
echo date("Y-m-d", $ts); // تاريخ الجمعة القادمة
?>
```

---

*🧠 5. getdate()
ترجع مصفوفة تحتوي على معلومات مفصلة عن التاريخ والوقت الحالي.

```php
<?php
print_r(getdate());
?>
```

---

*🧮 6. date_diff() و date_create()
لحساب الفرق بين تاريخين.

```php
<?php
$date1 = date_create("2025-12-01");
$date2 = date_create("2025-12-19");
$diff = date_diff($date1, $date2);
echo $diff->format("%a أيام"); // 18 أيام
?>
```

---

*🕰️ 7. sleep() و usleep()
لتأخير تنفيذ السكربت.

```php
<?php
sleep(3); // تأخير 3 ثواني
usleep(500000); // تأخير نصف ثانية (500,000 ميكروثانية)
?>
```

-----------------------------------------------------------------------------------

: دوال التعامل مع الملفات *

---

*📂 1. fopen()
لفتح ملف (للقراءة أو الكتابة أو الإضافة).

```php
<?php
$handle = fopen("example.txt", "r"); // فتح الملف للقراءة فقط
?>
```

:أنماط الفتح الشائعة* 
- "r" → قراءة فقط
- "w" → كتابة (يحذف المحتوى السابق)
- "a" → إضافة في نهاية الملف
- "r+" → قراءة وكتابة
- "a+" → قراءة وإضافة

---

*📖 2. fread() و fgets()
لقراءة محتوى الملف.

```php
<?php
$content = fread($handle, filesize("example.txt")); // قراءة كل المحتوى
$line = fgets($handle); // قراءة سطر واحد فقط
?>
```

---

*📝 3. fwrite()
للكتابة داخل الملف.

```php
<?php
$handle = fopen("example.txt", "w");
fwrite($handle, "مرحبًا بك في PHP!");
fclose($handle);
?>
```

---

*❌ 4. fclose()
لإغلاق الملف بعد الانتهاء.

```php
<?php
fclose($handle);
?>
```

---

*📄 5. file_get_contents() و file_put_contents()
طرق سريعة لقراءة أو كتابة ملف بالكامل.

```php
<?php
$data = file_get_contents("example.txt"); // قراءة الملف بالكامل
file_put_contents("example.txt", "نص جديد"); // كتابة واستبدال المحتوى
?>
```

---

*🧹 6. unlink()
لحذف ملف.

```php
<?php
unlink("example.txt");
?>
```

---

*📋 7. file_exists() و is_file()
للتحقق من وجود الملف.

```php
<?php
if (file_exists("example.txt")) {
    $size=
    file_exists("example.txt");
    
}
?>
```

---

*📁 8. mkdir() و rmdir()
لإنشاء أو حذف مجلد.

```php
<?php
mkdir("myfolder"); // إنشاء مجلد
rmdir("myfolder"); // حذف مجلد (يجب أن يكون فارغًا)
?>
```

---

*🧾 9. scandir()
لقراءة محتويات مجلد.

```php
<?php
$files = scandir("myfolder");
print_r($files);
?>
```

---

:🧠 مثال تطبيقي صغير*
```php
<?php
$filename = "log.txt";
if (!file_exists($filename)) {
    file_put_contents($filename, "تم إنشاء الملف\n");
} else {
    file_put_contents($filename, "تمت إضافة سطر جديد\n", FILE_APPEND);
}
?>
```

-------------------------------------------------------------------------------
: 'MySQL' طريقة الاتصال بقاعدة بيانات *

- يمكنك استخدام طريقتين شائعتين: 

---

*✅ الطريقة 1: باستخدام `MySQLi` (الأسهل والأكثر استخدامًا)*

```php
<?php
$host = "localhost";      // اسم المضيف (عادة localhost)
$user = "root";           // اسم المستخدم
$password = "";           // كلمة المرور
$database = "my_database"; // اسم قاعدة البيانات

// إنشاء الاتصال
$conn = new mysqli($host, $user, $password, $database);

// التحقق من الاتصال
if ($conn->connect_error) {
    die("فشل الاتصال: ". $conn->connect_error);
}

echo "تم الاتصال بنجاح!";
?>
```

*📌 ملاحظات:*
- تأكد من أن قاعدة البيانات موجودة.
- استخدم `utf8` إذا كنت تتعامل مع اللغة العربية:

```php
<?php
$conn->set_charset("utf8");
?>
```

---

🧠 `PDO` (أكثر أمانًا ومرونة) باستخدم الطريقة 2 :

```php
<?php
$host = "localhost";
$dbname = "my_database";
$user = "root";
$password = "";

try {
    $pdo = new PDO("mysql:host=$host;dbname=$dbname;charset=utf8", $user, $password);
    $pdo->setAttribute(PDO::ATTR_ERRMODE, PDO::ERRMODE_EXCEPTION);
    echo "تم الاتصال بنجاح!";
} catch (PDOException $e) {
    echo "فشل الاتصال: ". $e->getMessage();
}
?>
```

✅ مميزات `PDO`:
- يدعم أنواع قواعد بيانات متعددة (ليس فقط MySQL).
- أكثر أمانًا عند استخدام الاستعلامات المعدّة مسبقًا (prepared statements).

---
