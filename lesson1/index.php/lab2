(Array Functions) دوال المصفوفات

المصفوفات الأساسية:
<?php
// إنشاء مصفوفة
$fruits = array("تفاح", "موز", "برتقال");
// أو بالطريقة الحديثة
$colors = ["أحمر", "أخضر", "أزرق"];

// مصفوفة مرتبة (Associative Array)
$person = [
    "name" => "محمد",
    "age" => 25,
    "city" => "الرياض"
];
?>

-------------------------------------

اهم دوال المصفوفات *

1.count()-حساب عدد العناصر

$numbers = [1, 2, 3, 4, 5];
echo count($numbers); // الناتج: 5

2.array_push()-إضافة عنصر في النهاية

$stack = ["تفاح", "موز"];
array_push($stack, "برتقال", "فراولة");
print_r($stack);
// الناتج: Array ( [0] => تفاح [1] => موز [2] => برتقال [3] => فراولة )

3.array_pop()-إزالة عنصر من النهاية

$fruits = ["تفاح", "موز", "برتقال"];
$last_fruit = array_pop($fruits);
echo $last_fruit; // الناتج: برتقال

4.array_merge()-دمج مصفوفتبن

$array1 = ["أ", "ب"];
$array2 = ["ج", "د"];
$result = array_merge($array1, $array2);
print_r($result);
// الناتج: Array ( [0] => أ [1] => ب [2] => ج [3] => د )

5.array_search()-البحث في المصفوفة

$fruits = ["تفاح", "موز", "برتقال"];
$key = array_search("موز", $fruits);
echo $key; // الناتج: 1

6.in_array()-التحقق من وجود قيمة

$numbers = [1, 2, 3, 4, 5];
if (in_array(3, $numbers)) {
    echo "الرقم 3 موجود في المصفوفة";
}

7.array_key_exists()-التحقق من وجود مفتاح

$person = ["name" => "أحمد", "age" => 25];
if (array_key_exists("name", $person)) {
    echo "المفتاح name موجود";
}

8.sort() و rsort()-ترتيب المصفوفة

$numbers = [3, 1, 4, 2, 5];
sort($numbers);  // ترتيب تصاعدي
print_r($numbers); // [1, 2, 3, 4, 5]

rsort($numbers); // ترتيب تنازلي
print_r($numbers); // [5, 4, 3, 2, 1]

9.array_reverse()-عكس المصفوفة

$numbers = [1, 2, 3, 4, 5];
$reversed = array_reverse($numbers);
print_r($reversed); // [5, 4, 3, 2, 1]

10.array_slice()-استخراج جزء من المصفوفة

$numbers = [1, 2, 3, 4, 5];
$part = array_slice($numbers, 1, 3);
print_r($part); // [2, 3, 4]

############################################################

(String Functions) دوال النصوص

التعامل مع النصوص الأساسية :

<?php
$text = "مرحبا بالعالم";
$name = "محمد";
?>

-------------------------------------

أهم دوال النصوص *

1.strlen() - طول النص

$text = "مرحبا";
echo strlen($text); // الناتج: 10 (لأن الحروف العربية تأخذ 2 بايت)

2.mb_strlen() - طول النص مع الدعم للغات المختلفة

$text = "مرحبا";
echo mb_strlen($text, "UTF-8"); // الناتج: 5 (دقيق مع الحروف العربية)

3.str_replace() - استبدال النص

$text = "أنا أحب التفاح";
$new_text = str_replace("التفاح", "البرتقال", $text);
echo $new_text; // الناتج: أنا أحب البرتقال

4.strpos() - البحث عن موضع النص

$text = "مرحبا بالعالم";
$position = strpos($text, "بالعالم");
echo $position; // الناتج: 7

5.substr() - استخراج جزء من النص

$text = "مرحبا بالعالم";
$part = substr($text, 0, 5);
echo $part; // الناتج: مرحبا

6.explode() - تحويل النص الى مصفوفة

$text = "تفاح,موز,برتقال";
$fruits = explode(",", $text);
print_r($fruits);
// الناتج: Array ( [0] => تفاح [1] => موز [2] => برتقال )

7.implode() - تحويل المصفوفة الى نص

$fruits = ["تفاح", "موز", "برتقال"];
$text = implode(" - ", $fruits);
echo $text; // الناتج: تفاح - موز - برتقال

8.trim() - إزالة المسافات من الطرفين

$text = "   مرحبا   ";
echo "|" . trim($text) . "|"; // الناتج: |مرحبا|

9.strtolower() و strtoupper() - تحوبل الحروف

$text = "Hello World";
echo strtolower($text); // hello world
echo strtoupper($text); // HELLO WORLD

10.ucfirst() و ucwords() - تحوبل أول حرف إلى كبير

$text = "hello world";
echo ucfirst($text);  // Hello world
echo ucwords($text);  // Hello World

11.strrev() - عكس النص

$text = "مرحبا";
echo strrev($text); // الناتج: ابحرم

12.str_repeat() - تكرار النص

$text = "مرحبا ";
echo str_repeat($text, 3); // الناتج: مرحبا مرحبا مرحبا

############################################################

أمثلة عملية متكاملة *

مثال1:معالجة بيانات المستخدم

<?php
// بيانات المستخدمين
$users = [
    ["name" => "أحمد محمد", "email" => "ahmed@example.com"],
    ["name" => "فاطمة علي", "email" => "fatima@example.com"],
    ["name" => "خالد حسن", "email" => "khaled@example.com"]
];

// استخراج الأسماء فقط
$names = array_column($users, 'name');

// تحويل الأسماء إلى أحرف صغيرة للبحث
$search_name = "أحمد";
$found = false;

foreach ($names as $name) {
    if (strpos(strtolower($name), strtolower($search_name)) !== false) {
        $found = true;
        break;
    }
}

if ($found) {
    echo "تم العثور على المستخدم!";
}
?>

مثال2:تحليل النصوص

<?php
$text = "تفاح, موز, برتقال, فراولة, عنب";

// تنظيف النص وإزالة المسافات
$cleaned_text = str_replace(' ', '', $text);

// تحويل إلى مصفوفة
$fruits_array = explode(',', $cleaned_text);

// ترتيب أبجدي
sort($fruits_array);

// تحويل مرة أخرى إلى نص
$sorted_text = implode(' - ', $fruits_array);

echo $sorted_text; // الناتج: تفاح - برتقال - فراولة - عنب - موز
?>

مثال3:إنشاء تقرير

<?php
$students = [
    ["name" => "أحمد", "grades" => [85, 90, 78]],
    ["name" => "مريم", "grades" => [92, 88, 95]],
    ["name" => "خالد", "grades" => [76, 82, 79]]
];

foreach ($students as $student) {
    $average = array_sum($student['grades']) / count($student['grades']);
    $formatted_avg = number_format($average, 2);
    
    echo "الطالب: " . $student['name'] . "\n";
    echo "المعدل: " . $formatted_avg . "\n";
    echo "أعلى درجة: " . max($student['grades']) . "\n";
    echo "أقل درجة: " . min($student['grades']) . "\n";
    echo str_repeat("-", 30) . "\n";
}
?>

############################################################

:نصائح مهمة للمستخدم 🎯

1.mb_functions استخدم الدوال المناسبة للغة العربية مثال
2.تحقق دائماً من وجودالمفاتيح قبل استخدامها
3.للتحقق من وجودالقيمisset() و empty() استخدم
4.خاصة مع اللغة العربية(Encoding)تعامل بحذر مع الترميز 